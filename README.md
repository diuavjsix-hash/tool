# t·finder

누적확률 또는 유의수준과 자유도를 입력해 Student t 분포의 임계값을 계산하는 정적 웹 도구입니다.

## 기능

- 누적확률 `P(T ≤ t)`에서 t 값 계산
- 우측 유의수준 `α = P(T ≥ t)` 지원
- 양측 유의수준 `α = P(|T| ≥ t)` 지원
- Student t 분포 곡선과 확률 영역 시각화
- 외부 라이브러리나 서버 없이 브라우저에서 계산

GitHub Pages를 통해 배포됩니다.
