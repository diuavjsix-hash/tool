# 보안 정책

## 지원 범위

배포 사이트와 `main` 브랜치의 최신 코드만 유지보수 대상입니다. 이전 커밋이나 개인 포크에는 별도 보안 업데이트를 제공하지 않습니다.

## 취약점 제보

실행 가능한 코드 삽입, 의도하지 않은 외부 전송, 민감한 데이터 노출처럼 악용 가능한 문제는 공개 이슈에 상세 재현 정보나 비밀값을 올리지 마세요. 저장소의 GitHub Security 탭에서 비공개 취약점 제보가 활성화되어 있다면 해당 경로를 우선 사용해 주세요.

일반 계산 오류나 화면 문제는 공개 이슈 템플릿을 사용해도 됩니다. 실제 개인정보 대신 재현용 예시값을 사용합니다.

제보에는 가능한 범위에서 다음을 포함합니다.

- 영향받는 URL과 도구
- 재현에 필요한 최소 단계
- 예상 영향과 관찰한 동작
- 브라우저·운영체제
- 민감한 값을 제거한 로그 또는 화면

## 현재 보안 경계

- 백엔드, 로그인, 데이터베이스와 외부 계산 API가 없습니다.
- 입력과 최근 기록은 브라우저 메모리에만 있으며 새로고침하면 사라집니다.
- 계산 엔진은 `eval`, `new Function` 또는 임의 JavaScript 실행을 사용하지 않습니다.
- 외부 네트워크 요청, 분석 SDK, 쿠키와 브라우저 영구 저장을 사용하지 않습니다.

이 경계를 바꾸는 기능은 구현 전에 위협 모델, 개인정보 안내, 데이터 수명과 실패 시 동작을 문서화해야 합니다. 세부 유지보수 절차는 [유지보수·운영 문서](docs/MAINTENANCE.md)를 참고하세요.
