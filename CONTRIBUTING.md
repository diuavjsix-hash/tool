# tool·lab 기여 가이드

이 문서는 기능 추가, 버그 수정과 의존성 갱신을 같은 품질 기준으로 수행하기 위한 작업 순서입니다.

## 1. 개발 환경

- Node.js 24 (`.nvmrc` 기준)
- pnpm 11.9 이상 (`package.json#packageManager` 기준)
- Git

```bash
pnpm install --frozen-lockfile
pnpm dev
```

Vite 개발 서버가 표시하는 `/tool/` 주소에서 해시 경로를 확인합니다.

## 2. 변경 전 확인

1. `git status -sb`로 기존 작업을 확인합니다.
2. 계산 로직을 바꾼다면 [계산 계약](docs/CALCULATION_CONTRACTS.md)을 읽습니다.
3. 새 화면이나 도구라면 [아키텍처의 확장 절차](docs/ARCHITECTURE.md)를 확인합니다.
4. 현재 기준선으로 `pnpm check`가 통과하는지 확인합니다.

사용자의 다른 변경이 작업 트리에 있으면 임의로 덮어쓰거나 함께 커밋하지 않습니다.

## 3. 변경 원칙

### 계산 로직

- 계산 규칙은 `src/lib/`에 두고 React 화면에서 직접 구현하지 않습니다.
- `eval`, `Function` 생성자 또는 문자열 기반 동적 실행을 추가하지 않습니다.
- 정상값뿐 아니라 정의역, 경계값, 잘못된 토큰과 비유한 결과를 테스트합니다.
- 표시 반올림과 내부 계산 정밀도를 구분합니다.
- 사용자에게 보이는 오류는 한국어로 유지합니다.

### 화면과 상태

- 계산기 페이지는 `React.lazy`로 지연 로딩합니다.
- 서버 저장, 분석 도구, `localStorage` 또는 쿠키를 추가하려면 개인정보 정책과 UI 안내를 먼저 설계합니다.
- 키보드 포커스, 버튼의 접근 가능한 이름, `aria-live` 결과 알림과 `prefers-reduced-motion`을 유지합니다.
- 모바일과 데스크톱에서 결과가 가려지거나 가로 스크롤이 생기지 않는지 확인합니다.

### 문서

- 기능·계산 규칙·명령·파일 책임이 달라지면 같은 변경에서 문서를 갱신합니다.
- README는 입문과 문서 안내에 집중하고, 세부 규칙은 `docs/`에 둡니다.
- 로컬 Markdown 링크는 `pnpm check:docs`로 검증합니다.

## 4. 변경 유형별 필수 작업

| 변경 | 반드시 함께 확인할 항목 |
| --- | --- |
| t 분포 공식·범위 | `statistics.test.ts`, 계산 계약, 대표값 `p=0.975`, `df=10` |
| 수식 문법·연산자 | tokenizer/parser/evaluator 테스트, 오류 문구, 계산 계약 |
| 시각 수식 버튼 | MathLive 변환 테스트, 커서·선택 영역·분수 이동, 키보드 입력 |
| 새 도구 | 라우트, 제목, 홈·헤더 링크, 지연 로딩, 모바일 UI, 문서 |
| 의존성 | 잠금 파일, 전체 검사, 번들 예산, 개인정보·라이선스 영향 |
| 디자인 토큰 | 모든 페이지, 포커스 상태, 대비, 모션 감소와 모바일 |

## 5. 테스트

개발 중에는 관련 테스트를 반복 실행합니다.

```bash
pnpm test:watch
```

완료 전에는 반드시 다음을 실행합니다.

```bash
pnpm check
```

UI 변경은 자동 검사만으로 충분하지 않습니다. 최소 브라우저 확인 항목은 다음과 같습니다.

- 홈에서 각 도구로 이동하고 뒤로·앞으로 이동
- `/tool/#/t`에서 `p=0.975`, `df=10` 결과 `2.228139`
- `/tool/#/scientific`에서 `200 + 10% = 220`
- 루트·지수·분수가 실제 수학 표기로 보이고 방향키가 수식 구조를 이동
- 390px 안팎 모바일 너비에서 가로 넘침 없음
- 결과·오류 알림과 키보드 `Enter`, `Escape`
- 새로고침 후 계산 기록 초기화

## 6. 커밋과 Pull Request

- 한 커밋에는 하나의 설명 가능한 변경 목적을 담습니다.
- 생성물인 `dist/`와 로컬 캐시를 커밋하지 않습니다.
- PR 템플릿의 검사 항목을 완료하고 계산 동작 변경은 전후 예시를 적습니다.
- PR 품질 검사가 통과한 뒤 병합합니다.

## 7. 의존성 갱신

Dependabot이 월 1회 npm과 GitHub Actions 갱신 PR을 만듭니다. 갱신 시에는 다음을 확인합니다.

1. 릴리스 노트와 breaking change를 검토합니다.
2. `pnpm install` 후 잠금 파일 변경이 예상 범위인지 봅니다.
3. `pnpm check`와 브라우저 검증을 수행합니다.
4. MathLive 갱신은 수식 입력, LaTeX 변환, 방향키와 모바일을 별도로 확인합니다.

배포와 장애 대응은 [유지보수·운영 문서](docs/MAINTENANCE.md)를 따릅니다.
